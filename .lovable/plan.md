
# Улучшение отображения полей отзыва (как на Wildberries)

## Что изменится

Приведём отображение комментария, плюсов и недостатков к виду, максимально похожему на оригинал Wildberries (как на скриншоте).

## Визуальные изменения

### 1. Комментарий -- в кавычках, как основной текст

Сейчас комментарий выводится как `text-muted-foreground text-sm`. Будет оформлен как основной текст в кавычках:

```
"Очень рада покупке, спасибо!"
```

### 2. Плюсы -- с иконкой ThumbsUp

Вместо текстовой подписи "Плюсы:" будет иконка `ThumbsUp` из lucide-react зелёного цвета:

```
👍 Классная, приятно носить
```

### 3. Недостатки -- с иконкой ThumbsDown

Вместо текстовой подписи "Недостатки:" будет иконка `ThumbsDown` из lucide-react красного/оранжевого цвета:

```
👎 Всё хорошо!
```

### 4. Исправить устаревший тип статуса

В `ReviewCard` на строке 15 осталось `"new"` в типе статуса -- убрать для консистентности с остальным кодом.

## Затрагиваемый файл

| Файл | Изменение |
|------|-----------|
| `src/components/ReviewCard.tsx` | Обновить визуальное оформление полей отзыва; убрать "new" из типа статуса |

## Результат

Карточка отзыва будет выглядеть максимально приближённо к оригинальному интерфейсу Wildberries: комментарий в кавычках, плюсы с иконкой "палец вверх", недостатки с иконкой "палец вниз".
